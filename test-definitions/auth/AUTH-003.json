{
  "id": "AUTH-003",
  "author": "vjan-nie",
  "dependencies": [
    "postgres"
  ],
  "description": "GET to GoTrue user endpoint without Authorization header. Must return 401.",
  "domain": "auth",
  "environment": [
    "local"
  ],
  "expected": {
    "statusCode": 401
  },
  "method": "GET",
  "notes": "No Authorization header sent — GoTrue must reject with 401.",
  "phase": "phase-1",
  "preconditions": [
    "GoTrue running on :9999"
  ],
  "priority": "P1",
  "retries": 1,
  "service": "auth-service",
  "status": "active",
  "tags": [
    "gotrue",
    "jwt",
    "unauthorized"
  ],
  "timeout_ms": 5000,
  "title": "Request to protected endpoint without token returns 401",
  "type": "http",
  "url": "http://localhost:9999/user"
}
