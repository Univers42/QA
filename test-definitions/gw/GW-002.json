{
  "id": "GW-002",
  "title": "Kong rate limiting returns 429 after threshold exceeded",
  "description": "Send requests above the rate limit threshold to Kong auth route. Must return 429 Too Many Requests.",
  "domain": "gateway",
  "type": "integration",
  "layer": "infra",
  "priority": "P1",
  "tags": ["kong", "rate-limiting", "security"],
  "service": "api-gateway",
  "environment": ["local"],
  "dependencies": ["kong", "redis"],
  "preconditions": ["Kong running on :8000", "Rate limiting plugin configured on auth route", "Redis running on :6379"],
  "expected": {
    "statusCode": 429
  },
  "url": "http://localhost:8000/auth/health",
  "method": "GET",
  "author": "vjan-nie",
  "phase": "phase-0",
  "status": "draft",
  "notes": "Requires runner support for repeated requests. Single HTTP call cannot trigger rate limit alone."
}