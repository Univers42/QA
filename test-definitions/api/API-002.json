{
  "id": "API-002",
  "author": "dlesieur",
  "dependencies": [
    "mongodb-atlas"
  ],
  "description": "GET /tests must return a JSON response containing a 'tests' array and a 'total' count. Verifies the API can read from Atlas.",
  "domain": "api",
  "environment": [
    "local"
  ],
  "expected": {
    "statusCode": 200,
    "bodyContains": [
      "tests",
      "total"
    ]
  },
  "method": "GET",
  "notes": "Tests our own QA infrastructure. Requires make api to be running and Atlas populated.",
  "phase": "phase-2",
  "preconditions": [
    "make api running",
    "At least one test exists in Atlas"
  ],
  "priority": "P0",
  "retries": 1,
  "service": "qa-api",
  "status": "active",
  "tags": [
    "qa-api",
    "fastapi",
    "crud"
  ],
  "timeout_ms": 5000,
  "title": "QA API /tests endpoint returns test definitions",
  "type": "http",
  "url": "http://localhost:8000/tests"
}
