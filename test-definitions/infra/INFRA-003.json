{
  "id": "INFRA-003",
  "author": "vjan-nie",
  "dependencies": [
    "postgres"
  ],
  "description": "Verifies GoTrue health endpoint is reachable. Required before any auth test can run.",
  "domain": "infra",
  "environment": [
    "local"
  ],
  "expected": {
    "statusCode": 200,
    "bodyContains": [
      "version"
    ]
  },
  "method": "GET",
  "notes": "Requires CREATE SCHEMA auth in PostgreSQL before GoTrue can start.",
  "phase": "phase-0",
  "preconditions": [
    "mini-baas-infra services running",
    "auth schema created in PostgreSQL"
  ],
  "priority": "P0",
  "retries": 1,
  "service": "auth-service",
  "status": "active",
  "tags": [
    "gotrue",
    "auth",
    "smoke"
  ],
  "timeout_ms": 5000,
  "title": "GoTrue auth service responds on port 9999",
  "type": "http",
  "url": "http://localhost:9999/health"
}
