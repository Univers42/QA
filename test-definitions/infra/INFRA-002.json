{
  "id": "INFRA-002",
  "author": "vjan-nie",
  "dependencies": [],
  "description": "Verifies Kong proxy is reachable. All client traffic passes through Kong — if this fails, nothing works.",
  "domain": "infra",
  "environment": [
    "local"
  ],
  "expected": {
    "statusCode": 200
  },
  "method": "GET",
  "notes": "Set active once Kong is added to mini-baas-infra docker-compose.",
  "phase": "phase-0",
  "preconditions": [
    "Kong added to docker-compose and running"
  ],
  "priority": "P0",
  "retries": 1,
  "service": "api-gateway",
  "status": "draft",
  "tags": [
    "kong",
    "gateway",
    "smoke"
  ],
  "timeout_ms": 5000,
  "title": "Kong API gateway responds on port 8000",
  "type": "http",
  "url": "http://localhost:8000/"
}
