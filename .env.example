# ── Prismatica QA · Environment Variables ──────────────────────
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ── MongoDB Atlas (required) ───────────────────────────────────
# Get your connection string from: Atlas → Database → Connect → Drivers
# Replace <db_password> with your actual password
MONGO_URI_ATLAS=mongodb+srv://univers42:<db_password>@cluster0.rqo6vif.mongodb.net/test_hub?retryWrites=true&w=majority&appName=Cluster0

# ── Services under test (mini-baas-infra) ──────────────────────
KONG_URL=http://localhost:8000
GOTRUE_URL=http://localhost:9999
POSTGREST_URL=http://localhost:3000
REALTIME_URL=ws://localhost:4000
MINIO_URL=http://localhost:9000
FRONTEND_URL=http://localhost:3000

# ── API server ─────────────────────────────────────────────────
API_URL=http://localhost:8000
API_PORT=8000
