# Prismatica QA — Python dependencies
# Install: pip install -r requirements.txt

# --- Core ---
pymongo>=4.6.0              # MongoDB driver (Atlas connection)
pydantic>=2.5.0             # Schema validation (replaces AJV)
python-dotenv>=1.0.0        # Load .env variables

# --- API ---
fastapi>=0.109.0            # REST API framework
uvicorn[standard]>=0.27.0   # ASGI server for FastAPI
websockets>=12.0            # WebSocket support for live test execution

# --- Runner ---
httpx>=0.26.0               # Async HTTP client (service calls)

# --- CLI ---
typer[all]>=0.9.0           # CLI framework (subcommands, autocomplete)
rich>=13.0.0                # Terminal UI (tables, colors, progress bars)
